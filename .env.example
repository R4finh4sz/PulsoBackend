DB_URL=jdbc:postgresql://localhost:5432/teste.
DB_USERNAME=
DB_PASSWORD=

# Brevo SMTP login and SMTP key (not the API key)
BREVO_SMTP_HOST=smtp-relay.brevo.com
BREVO_SMTP_PORT=000
BREVO_SMTP_USERNAME=
BREVO_SMTP_PASSWORD=
BREVO_FROM_EMAIL=
# Base64 of at least 32 random bytes; keep private and stable across instances.
JWT_SECRET=
JWT_SESSION_TTL=30m
